# Hello Page

This is a simple example page to demonstrate Nextra's capabilities.

## Code Example

```javascript
function sayHello() {
  console.log("Hello, world!");
}

sayHello();
```

## Interactive Components

Nextra supports React components in MDX:

import { useState } from 'react'

export function Counter() {
  const [count, setCount] = useState(0)
  return (
    <div>
      <button onClick={() => setCount(count + 1)}>
        Clicked {count} times
      </button>
    </div>
  )
}

<Counter />

## Next Steps

Return to the [home page](/) to explore more features.